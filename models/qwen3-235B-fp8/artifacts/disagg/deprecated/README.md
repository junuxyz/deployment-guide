# Deprecated Qwen3-235B disaggregated artifacts

`pre-rdma/` contains the original disaggregated concurrency-32 benchmark. This run failed to connect and use RoCE/RDMA properly and preserved only for reference.

The correct (RDMA-enabled) run is the active artifact at [`../concurrency_32/`](../concurrency_32/).
